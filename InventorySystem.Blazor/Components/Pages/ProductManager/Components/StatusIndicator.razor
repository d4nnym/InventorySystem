<MudChip T="string" Color="@Color">@Label</MudChip>

@code {
    [Parameter] public Guid? Value { get; set; }
    [Parameter] public string Label { get; set; } = string.Empty;

    private Color Color => Value is null ? Color.Warning : Color.Success;
}